{
  "private": false,
  "name": "che-kung-api",
  "version": "1.0.0",
  "description": "An API to retrieve the Che Kung Fortune stick ",
  "main": "bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "pack": "browserify index.js -o bundle.js"
  },
  "author": "Norman Kong",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
